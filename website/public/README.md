# Brand assets

Image placeholder files were removed intentionally because they block PR creation in the current workflow.

When brand assets are ready, add these files manually under `website/public/` and then uncomment references in `src/components/SiteHead.astro` and `src/pages/index.astro`:

- `favicon.svg`
- `favicon.ico`
- `og-image.png`
